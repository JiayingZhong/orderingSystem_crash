spring:
  application:
    name: order
  profiles:
    active: dev

  cloud:
    config:
      uri: http://localhost:8762
      fail-fast: true
#  cloud:
#    config:
#      uri: http://localhost:8762
#      label: master
#      name: order
#      fail-fast: false